<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="@layout/layout_navigation_drawer">

    <ImageView
        android:id="@+id/nav_header_app_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/nav_drawer_header_text_spacing"
        android:layout_toEndOf="@id/nav_header_app_icon"
        android:orientation="vertical">

        <TextView
            android:id="@+id/nav_header_app_name"
            style="@style/TextStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/nav_drawer_header_text_margin"
            android:text="@string/nav_header_title"
            android:textSize="@dimen/text_size_large" />

        <TextView
            android:id="@+id/nav_header_app_version"
            style="@style/TextStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/nav_header_title" />

    </LinearLayout>

</merge>
